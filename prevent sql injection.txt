1. 5' or 1=1
   5 or "="
   7 or 2=2

2. 5' or "='

3. 7' or 1=1  --  
   9' or "="  --  
   sfsd' or 'tr'='tr'  --  


using prepared statement
$query ="SELECT users.id, users.first_name ,users.last_name, users.email, role.name as role_name FROM users " .
"INNER JOIN roles ON users.role_id=roles.id " .
        "WHERE users.email=? "
            "AND users.password=? ";
$password=md5($password);
$preparedStatement=$con->prepare($query);
$preparedStatement->bind_param('ss', $username, $password);
$preparedStatement->execute();
$result=$preparedStatement->get_result();

prevent XSS Attacks

<?=htmlspacialchars($_GET['name'],ENT_QOUTES,'UTF-8')?>

the predefined characters are:

1. &(ampersand) becomes &apm;
2. "(double quote) becomes &quote;
3. ' (single quote) becomes &#039;
4. <(less than) becomes &lt;
5. >(grather than) becomes &gt;


 $title =htmlspacialchars($_POST["title"],ENT_QUOTES, 'UTF-8');

$description=htmlspecialchars($_POST["description"],ENT_QUOTES, 'UTF-8');

description description description
 description
description
<script>
alert("stored xss attack");
</script>